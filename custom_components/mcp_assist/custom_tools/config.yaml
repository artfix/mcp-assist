# Custom tools configuration for ha-lmstudio-mcp
# Enable/disable custom tools here

enabled_tools:
  - brave_search
  - read_url
  # - vision  # Uncomment when ready

# Tool-specific settings (if needed)
brave_search:
  max_results: 10

read_url:
  max_content_length: 50000
  timeout: 15